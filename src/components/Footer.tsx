import React from 'react';
import styled from "styled-components";

const StyledFooter = styled.footer`
  font-size: 12px;
  margin: 30px 120px;
  text-align: center;
  font-weight: bold;
`;

export const Footer = () => <StyledFooter>Made with ❤️ in Leipzig, Germany</StyledFooter>;
